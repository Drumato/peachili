use crate::compiler::arch::x64;

pub fn codegen_main(ast_root: x64::Root) -> String {
    let mut assembly_file = String::new();

    assembly_file
}

fn gen_fn(mut assembly_file: String) -> String {
    assembly_file
}
